apiVersion: v1
kind: Service
metadata:
  name: telemetry-pipeline
  namespace: telemetry

spec:
  selector:
    app: telemetry-pipeline

  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
